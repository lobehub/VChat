import { createStyles } from 'antd-style';

const useStyles = createStyles(({ css }) => ({
  counter: css`
    font-size: 12px;
  `,
  duration: css`
    display: flex;
    justify-content: center;
    align-items: center;
  `,
}));

export { useStyles };
